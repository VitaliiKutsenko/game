import styled from 'styled-components';

export const HeaderWrapper = styled.header`
  position: sticky;
  z-index: 1000;
  top: 0;
  width: 100%;
  height: auto;
  display: flex;
  overflow: hidden;
`;
